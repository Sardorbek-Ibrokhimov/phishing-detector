# Runtime dependencies for the DEPLOYED API and the test suite.
# Pinned to the versions the model was trained/verified against.
#
# NOTE: shap and matplotlib are deliberately NOT here. The served API computes
# SHAP values via XGBoost's native TreeSHAP (no `shap` import at runtime), which
# keeps the process under Render's 512 MB free tier. shap pulls in numba +
# llvmlite + matplotlib (~150 MB) that the runtime never uses. Those live in
# requirements-dev.txt, needed only to regenerate the SHAP summary plot or run
# the analysis scripts in src/ (review_experiments.py, accuracy_sanity.py).
fastapi==0.139.2
uvicorn==0.51.0
httpx==0.28.1
joblib==1.5.3
tldextract==5.3.1
xgboost==3.3.0
scikit-learn==1.9.0
numpy==2.4.6
pandas==3.0.3
scipy==1.18.0
pydantic==2.13.4
